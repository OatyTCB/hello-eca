name: gateway
version: "1.1.0"
team: frontend
contract: gateway.yaml

protocols:
  rest:
    contract: generated/openapi.yaml
    base-path: /api/v1/gateway

consumes:
  - module: greeter
    version: ">=2.0.0 <3.0.0"
    protocol: grpc
    usage: usage/greeter.usage.yaml
