name: main
description: "Both modules in one cell — in-process calls, zero network overhead"

modules:
  - ref: modules/greeter/v2/module.yaml
  - ref: modules/gateway/v1/module.yaml

resources:
  cpu: "1"
  memory: 512Mi
